# hermes/ 三个 bridge(mcp_bridge / analyze_bridge / workflow_bridge)的运行与测试依赖。
#
# 安装: pip install -r hermes/requirements.txt
# 测试: python -m pytest hermes
#
# mcp 必须锁版本(A11):2.x 已移除 mcp.server.fastmcp,而 mcp_bridge.py 从该路径
# 导入 FastMCP 与 TransportSecuritySettings。不锁版本直接 `pip install mcp` 会装到
# 2.x 并在 import 阶段就 ModuleNotFoundError。升 2.x 需先评估替代 API,属独立改动。
mcp==1.29.0

# Runtime 受控接口调用(mcp_bridge)与测试里的 MockTransport 都来自 httpx
httpx>=0.27
# analyze_bridge / workflow_bridge 的 HTTP 服务端
fastapi>=0.110
uvicorn>=0.29
# analyze_bridge 对 LLM 输出做 Schema 校验(契约不过打回重试)
jsonschema>=4.21
pyyaml>=6.0

pytest>=8.0
